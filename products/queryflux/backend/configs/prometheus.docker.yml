# Minimal Prometheus config for docker-compose (no K8s SD, no missing rule_files).
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: queryflux-backend
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ['backend:8080']
