/* commands.js — function-command handlers (no UI). Currently empty;
 * the taskpane button handles all user interactions. Kept as the
 * AppSource-required entry point so future "scrub and send"-style
 * direct-action ribbon buttons can land here without manifest churn.
 */

Office.onReady(() => {
  // Register direct-action handlers here when we add them, e.g.:
  //   Office.actions.associate("scrubAndSend", async (event) => { ... });
});
