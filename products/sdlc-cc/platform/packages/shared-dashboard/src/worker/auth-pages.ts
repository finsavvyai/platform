// Authentication Pages - Re-export barrel for backward compatibility
// Individual pages split into separate modules

export { registerPageHTML } from './register-page';
export { loginPageHTML } from './login-page';
