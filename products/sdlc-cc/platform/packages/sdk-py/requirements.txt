# Runtime requirements mirroring [project.dependencies] in pyproject.toml.
# Kept as a separate file so CI tooling (pip-audit, pushci pip-install
# checks, Docker builds) can consume without running `pip install -e .`
httpx>=0.25.0
pydantic>=2.4.0
pydantic-settings>=2.0.0
tenacity>=8.2.0
structlog>=23.1.0
cryptography>=41.0.0
python-jose[cryptography]>=3.3.0
typing-extensions>=4.7.0
aiolimiter>=1.1.0
orjson>=3.9.0
python-dateutil>=2.8.2
pytz>=2023.3
