apiVersion: v1
kind: Service
metadata:
  name: {{ include "sdlc-gateway.fullname" . }}
  labels:
    {{- include "sdlc-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sdlc-gateway.selectorLabels" . | nindent 4 }}
