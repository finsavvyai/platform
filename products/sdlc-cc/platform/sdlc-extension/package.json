{
  "name": "sdlc-extension",
  "version": "0.1.0",
  "private": true,
  "description": "SDLC Guard — Chrome/Edge MV3 extension for local PII redaction across ChatGPT, Claude, Gemini, Copilot.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "tsx --test src/pii-scan.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2"
  }
}
