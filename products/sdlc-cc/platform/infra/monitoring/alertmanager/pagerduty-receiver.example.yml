# Example: add this receiver to alertmanager.yml and route critical alerts to it.
# 1. Create a Prometheus integration in PagerDuty and copy the Integration Key.
# 2. Set PAGERDUTY_INTEGRATION_KEY in your secret store and inject at deploy time.
# 3. In route.routes, add or change critical route to use receiver: 'pagerduty-critical'
#
# receivers:
#   - name: 'pagerduty-critical'
#     pagerduty_configs:
#       - service_key: '<YOUR_PAGERDUTY_INTEGRATION_KEY>'
#         description: '{{ .GroupLabels.alertname }}: {{ .CommonAnnotations.summary }}'
#         details:
#           firing: '{{ .Alerts.Firing | len }}'
#           num_firing: '{{ .Alerts.Firing | len }}'
#           num_resolved: '{{ .Alerts.Resolved | len }}'
