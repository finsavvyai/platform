{
  "name": "@sdlc-platform/tests-qestro",
  "version": "0.1.0",
  "description": "Qestro-flavoured Playwright + REST tests for the privacy gateway, browser extension, and Trust Center.",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test api",
    "test:browser": "playwright test browser",
    "test:smoke": "playwright test smoke",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
