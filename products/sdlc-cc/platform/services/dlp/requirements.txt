# Runtime requirements for the DLP service.
# Authoritative source is pyproject.toml; this file exists for tooling
# (pip-audit, CI scans, Docker builds) that expects a requirements.txt.

# Structured LLM outputs
instructor>=1.0.0
openai>=1.14.0
anthropic>=0.25.0
pydantic>=2.5.0

# Test runner — required for `pytest` CI step. Kept here (not in a dev
# extras group) so pushci's `pip install -r requirements.txt` picks it up.
pytest>=7.4.0
