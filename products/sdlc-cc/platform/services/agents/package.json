{
  "name": "sdlc-agents",
  "version": "1.0.0",
  "description": "LAM agents for SDLC.ai — tool registry, base agent, specialized roles",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.js"],
    "collectCoverageFrom": [
      "*.js",
      "!**/node_modules/**"
    ]
  }
}
