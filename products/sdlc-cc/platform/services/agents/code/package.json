{
  "name": "@sdlc/agent-code",
  "version": "0.1.0",
  "private": true,
  "description": "SDLC code-action agent — applies patches, runs tests, opens PRs. REAL: docker-sandboxed tools + GitHub App PR creation.",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test src/*.test.js src/tools/*.test.js"
  }
}
