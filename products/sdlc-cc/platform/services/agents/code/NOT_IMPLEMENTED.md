# Moved -> STATUS.md

This package is no longer a SCAFFOLD. The three tools are real
(docker-backed `apply_patch` + `run_tests`, GitHub App `open_pr`).
See `STATUS.md` for runtime requirements and the test layout.
