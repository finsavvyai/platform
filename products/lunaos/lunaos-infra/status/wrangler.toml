name = "lunaos-status"
compatibility_date = "2024-11-01"

# Deploy as Cloudflare Pages (static site)
# Run: npx wrangler pages deploy . --project-name=lunaos-status
# Or connect via Cloudflare Dashboard → Pages → Create project

# Custom domain: status.lunaos.ai
# Configure in Cloudflare Dashboard → Pages → lunaos-status → Custom domains

[site]
bucket = "."
