import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Google Workspace OIDC Setup","description":"Connect Google Workspace to LunaOS as an OIDC Identity Provider.","frontmatter":{"title":"Google Workspace OIDC Setup","description":"Connect Google Workspace to LunaOS as an OIDC Identity Provider."},"headers":[],"relativePath":"guide/sso/google-oidc.md","filePath":"guide/sso/google-oidc.md"}');
const _sfc_main = { name: "guide/sso/google-oidc.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="google-workspace-oidc-setup" tabindex="-1">Google Workspace OIDC Setup <a class="header-anchor" href="#google-workspace-oidc-setup" aria-label="Permalink to &quot;Google Workspace OIDC Setup&quot;">​</a></h1><p>This guide walks a Google Workspace admin and a LunaOS org admin through setting up OIDC-based single sign-on using Google as the Identity Provider.</p><div class="info custom-block"><p class="custom-block-title">Why OIDC for Google?</p><p>Google recommends OAuth 2.0 / OIDC for its own integrations. While Google does support SAML, the OIDC path offers simpler configuration, standard discovery, and automatic key rotation via Google&#39;s JWKS endpoint. LunaOS recommends OIDC for all Google Workspace integrations.</p></div><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><ul><li><strong>Google</strong>: Google Workspace admin access (or a standard Google account for testing with External user type).</li><li><strong>Google Cloud</strong>: Access to a Google Cloud project associated with your Workspace domain.</li><li><strong>LunaOS</strong>: Org Admin role on the Team or Enterprise plan.</li></ul><hr><h2 id="step-1-—-create-an-oauth-client-id" tabindex="-1">Step 1 — Create an OAuth client ID <a class="header-anchor" href="#step-1-—-create-an-oauth-client-id" aria-label="Permalink to &quot;Step 1 — Create an OAuth client ID&quot;">​</a></h2><ol><li>Go to the <a href="https://console.cloud.google.com/" target="_blank" rel="noreferrer">Google Cloud Console</a> and select or create a project linked to your Workspace domain.</li><li>In the left menu, go to <strong>APIs &amp; Services → Credentials</strong>.</li><li>Click <strong>Create Credentials → OAuth client ID</strong>.</li><li>Set <strong>Application type</strong> to <strong>Web application</strong>.</li><li>Enter a name — for example, <code>LunaOS SSO</code>.</li></ol><hr><h2 id="step-2-—-set-the-authorized-redirect-uri" tabindex="-1">Step 2 — Set the authorized redirect URI <a class="header-anchor" href="#step-2-—-set-the-authorized-redirect-uri" aria-label="Permalink to &quot;Step 2 — Set the authorized redirect URI&quot;">​</a></h2><p>In the <strong>Authorized redirect URIs</strong> section, click <strong>Add URI</strong> and enter:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://api.lunaos.ai/v1/sso/oidc/callback</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>The redirect URI must match exactly — including the <code>https://</code> scheme, the full hostname, and the path. A trailing slash or typo will cause <code>redirect_uri_mismatch</code> errors.</p></div><p>Click <strong>Create</strong>.</p><hr><h2 id="step-3-—-copy-your-credentials" tabindex="-1">Step 3 — Copy your credentials <a class="header-anchor" href="#step-3-—-copy-your-credentials" aria-label="Permalink to &quot;Step 3 — Copy your credentials&quot;">​</a></h2><p>After creating the client, Google displays a dialog with:</p><ul><li><strong>Client ID</strong> — a string ending in <code>.apps.googleusercontent.com</code></li><li><strong>Client secret</strong> — a short alphanumeric secret</li></ul><p>Copy both values and store them securely. The client secret is shown only once in this dialog; retrieve it later from the Credentials page if needed.</p><hr><h2 id="step-4-—-configure-the-oauth-consent-screen" tabindex="-1">Step 4 — Configure the OAuth consent screen <a class="header-anchor" href="#step-4-—-configure-the-oauth-consent-screen" aria-label="Permalink to &quot;Step 4 — Configure the OAuth consent screen&quot;">​</a></h2><div class="info custom-block"><p class="custom-block-title">INFO</p><p>If your consent screen is already configured, verify the scopes below are present and skip to Step 5.</p></div><ol><li>In <strong>APIs &amp; Services → OAuth consent screen</strong>, click <strong>Edit App</strong> (or begin configuration if this is new).</li><li>Set <strong>User type</strong>: <ul><li><strong>Internal</strong> — recommended for Google Workspace organizations. Only users within your Workspace domain can authenticate. No review required.</li><li><strong>External</strong> — allows any Google account; requires Google review for scopes beyond the basic profile.</li></ul></li><li>Fill in the required fields (app name, support email, developer contact).</li><li>On the <strong>Scopes</strong> step, add the following scopes: <ul><li><code>openid</code></li><li><code>https://www.googleapis.com/auth/userinfo.email</code></li><li><code>https://www.googleapis.com/auth/userinfo.profile</code></li></ul></li><li>Under <strong>Authorized domains</strong>, add <code>lunaos.ai</code>.</li><li>Save and continue through the remaining steps.</li></ol><hr><h2 id="step-5-—-note-the-discovery-and-issuer-urls" tabindex="-1">Step 5 — Note the discovery and issuer URLs <a class="header-anchor" href="#step-5-—-note-the-discovery-and-issuer-urls" aria-label="Permalink to &quot;Step 5 — Note the discovery and issuer URLs&quot;">​</a></h2><p>Google publishes a standard OIDC discovery document. You do not need to configure these manually; copy them as-is into LunaOS:</p><table tabindex="0"><thead><tr><th>Field</th><th>Value</th></tr></thead><tbody><tr><td>Discovery URL</td><td><code>https://accounts.google.com/.well-known/openid-configuration</code></td></tr><tr><td>Issuer</td><td><code>https://accounts.google.com</code></td></tr></tbody></table><hr><h2 id="step-6-—-configure-in-lunaos" tabindex="-1">Step 6 — Configure in LunaOS <a class="header-anchor" href="#step-6-—-configure-in-lunaos" aria-label="Permalink to &quot;Step 6 — Configure in LunaOS&quot;">​</a></h2><ol><li>In the LunaOS dashboard, go to <strong>Organization Settings → SSO</strong>.</li><li>Click <strong>New SSO Configuration</strong>.</li><li>Select <strong>OIDC</strong> as the protocol.</li><li>Fill in the fields:</li></ol><table tabindex="0"><thead><tr><th>LunaOS field</th><th>Value</th></tr></thead><tbody><tr><td>Client ID</td><td>From Step 3</td></tr><tr><td>Client Secret</td><td>From Step 3</td></tr><tr><td>Discovery URL</td><td><code>https://accounts.google.com/.well-known/openid-configuration</code></td></tr><tr><td>Issuer</td><td><code>https://accounts.google.com</code></td></tr><tr><td>Email Domain</td><td>Your Workspace primary domain, e.g., <code>acme.com</code></td></tr><tr><td>Default Role</td><td><code>member</code> (recommended for new JIT users)</td></tr></tbody></table><ol start="5"><li>Click <strong>Save</strong>.</li></ol><div class="tip custom-block"><p class="custom-block-title">Hosted-domain enforcement</p><p>Google includes an <code>hd</code> (hosted domain) claim in the ID token for Workspace users. LunaOS validates this claim against your configured <code>emailDomain</code>. Set <code>emailDomain</code> to your Workspace primary domain to ensure only users from that domain can authenticate — even if another Google account with the same email exists.</p></div><hr><h2 id="step-7-—-test-the-connection" tabindex="-1">Step 7 — Test the connection <a class="header-anchor" href="#step-7-—-test-the-connection" aria-label="Permalink to &quot;Step 7 — Test the connection&quot;">​</a></h2><ol><li>In <strong>Organization Settings → SSO</strong>, find your new configuration and click <strong>Test SSO</strong>.</li><li>Enter an email address from your Google Workspace domain.</li><li>LunaOS redirects you to Google&#39;s OAuth consent / login page.</li><li>Authenticate with your Workspace credentials.</li><li>On success, you are redirected back to the LunaOS dashboard.</li></ol><p>If the test fails, note the error code and consult the <a href="./troubleshooting.html">SSO Troubleshooting Guide</a>.</p><hr><h2 id="notes-on-multi-domain-workspace-organizations" tabindex="-1">Notes on multi-domain Workspace organizations <a class="header-anchor" href="#notes-on-multi-domain-workspace-organizations" aria-label="Permalink to &quot;Notes on multi-domain Workspace organizations&quot;">​</a></h2><p>If your organization has multiple primary domains in Google Workspace (e.g., <code>acme.com</code> and <code>acme.co.uk</code>), create a separate LunaOS SSO configuration for each domain. Each configuration uses the same Client ID and Client Secret but a different <code>emailDomain</code> value.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/sso/google-oidc.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const googleOidc = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  googleOidc as default
};
