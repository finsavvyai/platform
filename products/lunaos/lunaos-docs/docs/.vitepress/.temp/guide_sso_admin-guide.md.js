import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Enterprise SSO — Admin Guide","description":"Configure SAML 2.0 or OIDC single sign-on for your LunaOS organization.","frontmatter":{"title":"Enterprise SSO — Admin Guide","description":"Configure SAML 2.0 or OIDC single sign-on for your LunaOS organization."},"headers":[],"relativePath":"guide/sso/admin-guide.md","filePath":"guide/sso/admin-guide.md"}');
const _sfc_main = { name: "guide/sso/admin-guide.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="enterprise-sso-—-admin-guide" tabindex="-1">Enterprise SSO — Admin Guide <a class="header-anchor" href="#enterprise-sso-—-admin-guide" aria-label="Permalink to &quot;Enterprise SSO — Admin Guide&quot;">​</a></h1><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Enterprise SSO lets your organization authenticate LunaOS users through your own Identity Provider (IdP). Org admins configure one IdP per organization; LunaOS then routes login attempts to that IdP automatically based on email-domain discovery.</p><p><strong>Who this is for:</strong> Organization admins on the Team or Enterprise plan.</p><p><strong>What SSO adds:</strong></p><ul><li><strong>Per-org IdP binding</strong> — each organization connects its own SAML 2.0 or OIDC IdP.</li><li><strong>Just-in-time (JIT) user provisioning</strong> — new users are created automatically on first login; no manual invitation required.</li><li><strong>Email-domain discovery</strong> — LunaOS detects the user&#39;s domain and redirects to the correct IdP without the user selecting one.</li><li><strong>Fallback login</strong> — if SSO is not configured for an org, users can still log in with an API key. Existing API key sessions are not revoked when SSO is enabled.</li></ul><div class="info custom-block"><p class="custom-block-title">Supported protocols</p><p>LunaOS supports SAML 2.0 (SP-initiated) and OIDC (Authorization Code flow with PKCE). SAML is the default recommendation for enterprise IdPs such as Okta and Microsoft Entra ID. OIDC is the preferred choice for Google Workspace.</p></div><hr><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><p>The sequence below describes SP-initiated SSO login.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">sequenceDiagram</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    actor User</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    participant Browser</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    participant LunaOS as LunaOS API&lt;br/&gt;(api.lunaos.ai)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    participant IdP as Your IdP</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    User-&gt;&gt;Browser: Enter email on login page</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    Browser-&gt;&gt;LunaOS: POST /v1/auth/sso/discover { email }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    LunaOS--&gt;&gt;Browser: { redirect_url: &lt;IdP SSO URL&gt; }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    Browser-&gt;&gt;IdP: GET &lt;IdP SSO URL&gt; (AuthnRequest)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    IdP--&gt;&gt;User: Show IdP login UI</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    User-&gt;&gt;IdP: Authenticate (password, MFA, etc.)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    IdP--&gt;&gt;Browser: POST /v1/sso/saml/callback (SAMLResponse)&lt;br/&gt;or GET /v1/sso/oidc/callback?code=…</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    Browser-&gt;&gt;LunaOS: Forward assertion / code</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    LunaOS-&gt;&gt;LunaOS: Validate assertion, resolve or create user (JIT)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    LunaOS--&gt;&gt;Browser: Set session cookie, redirect to dashboard</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    Browser-&gt;&gt;User: Dashboard loaded</span></span></code></pre></div><hr><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><p>Before enabling SSO, confirm the following.</p><p><strong>LunaOS side:</strong></p><ul><li>You hold the <strong>Org Admin</strong> role in your organization.</li><li>Your organization is on the <strong>Team</strong> or <strong>Enterprise</strong> plan.</li><li>You own the email domain(s) you intend to lock to SSO (e.g., <code>acme.com</code>).</li></ul><p><strong>IdP side:</strong></p><ul><li>You have admin access to your IdP.</li><li>Your IdP supports <strong>SAML 2.0</strong> (SP-initiated) or <strong>OIDC</strong> (Authorization Code).</li><li>For SAML: your IdP can export a metadata XML or individual certificate + URLs.</li><li>For OIDC: your IdP supports a discovery endpoint (<code>/.well-known/openid-configuration</code>).</li></ul><hr><h2 id="enabling-sso" tabindex="-1">Enabling SSO <a class="header-anchor" href="#enabling-sso" aria-label="Permalink to &quot;Enabling SSO&quot;">​</a></h2><ol><li>In the LunaOS dashboard, navigate to <strong>Organization Settings → SSO</strong>.</li><li>Click <strong>New SSO Configuration</strong>.</li><li>Select your protocol: <strong>SAML 2.0</strong> or <strong>OIDC</strong>.</li><li>Follow the provider-specific setup guide: <ul><li><a href="./okta-saml.html">Okta (SAML)</a></li><li><a href="./azure-ad-saml.html">Microsoft Entra ID / Azure AD (SAML)</a></li><li><a href="./google-oidc.html">Google Workspace (OIDC)</a></li></ul></li><li>After saving, enter a test email from your domain and click <strong>Test SSO</strong>. A passing test confirms the IdP round-trip works before you enforce login.</li><li>Set <strong>Enforce SSO</strong> to <strong>On</strong> to require all users on the configured email domain to authenticate via the IdP.</li></ol><div class="warning custom-block"><p class="custom-block-title">Test before enforcing</p><p>Always run the SSO test in step 5 before toggling Enforce SSO. If enforcement is enabled while SSO is misconfigured, users on your domain will be unable to log in. Your org admin API key remains valid as a recovery path.</p></div><hr><h2 id="managing-jit-provisioned-users" tabindex="-1">Managing JIT-provisioned users <a class="header-anchor" href="#managing-jit-provisioned-users" aria-label="Permalink to &quot;Managing JIT-provisioned users&quot;">​</a></h2><p>When a user logs in via SSO for the first time, LunaOS creates their account automatically using the <code>email</code>, <code>firstName</code>, and <code>lastName</code> attributes from the IdP assertion.</p><p><strong>Default role assignment:</strong></p><p>JIT-provisioned users receive the role set in your SSO configuration field <code>defaultRole</code>. Allowed values: <code>member</code>, <code>viewer</code>. Org admins are never provisioned automatically; grant the admin role manually after the user&#39;s first login.</p><p><strong>Identifying JIT-provisioned users:</strong></p><p>In <strong>Organization Settings → Members</strong>, JIT-provisioned accounts show the badge <strong>SSO</strong> next to their name. You can filter by this badge to review the list.</p><p><strong>Changing a user&#39;s role:</strong></p><ol><li>Go to <strong>Organization Settings → Members</strong>.</li><li>Find the user and click <strong>Edit</strong>.</li><li>Update the <strong>Role</strong> field and save.</li></ol><p><strong>Removing access:</strong></p><p>There are two ways to revoke a user&#39;s access:</p><table tabindex="0"><thead><tr><th>Method</th><th>Effect</th></tr></thead><tbody><tr><td>Disable the user in your IdP</td><td>Blocks future logins. Active LunaOS sessions expire at their natural TTL (24 h).</td></tr><tr><td>Remove the user in LunaOS dashboard</td><td>Immediately removes org membership. The user cannot log in even if the IdP account is active.</td></tr></tbody></table><p>For immediate revocation, use both methods together.</p><hr><h2 id="disabling-sso" tabindex="-1">Disabling SSO <a class="header-anchor" href="#disabling-sso" aria-label="Permalink to &quot;Disabling SSO&quot;">​</a></h2><ol><li>Go to <strong>Organization Settings → SSO</strong>.</li><li>Toggle <strong>Enabled</strong> to <strong>Off</strong> and confirm.</li></ol><p>Existing SSO sessions remain valid until their TTL expires (24 hours); they are not forcibly terminated. Users who log in after SSO is disabled will see the standard login form and can authenticate with an API key or password-reset email.</p><p>API key authentication is always available, independent of SSO state.</p><hr><h2 id="security-best-practices" tabindex="-1">Security best practices <a class="header-anchor" href="#security-best-practices" aria-label="Permalink to &quot;Security best practices&quot;">​</a></h2><p><strong>Certificate rotation (SAML):</strong></p><p>Rotate your IdP&#39;s signing certificate every 365 days. When a new certificate is issued:</p><ol><li>Upload the new certificate to your LunaOS SSO configuration.</li><li>LunaOS supports two active certificates simultaneously during the rotation window.</li><li>Remove the old certificate after confirming all assertions are signed with the new one.</li></ol><p><strong>Secret rotation (OIDC):</strong></p><p>Rotate your OAuth client secret at least annually, or immediately after any suspected exposure:</p><ol><li>Generate a new secret in your IdP.</li><li>Update the <strong>Client Secret</strong> field in <strong>Organization Settings → SSO</strong>.</li><li>Save and run the SSO test to confirm the new secret works before revoking the old one.</li></ol><p><strong>Email domain restriction:</strong></p><p>Set <code>emailDomain</code> to a domain you own and control. Avoid wildcards. If your org uses multiple domains (e.g., <code>acme.com</code> and <code>acme.co.uk</code>), create a separate SSO configuration for each domain.</p><p><strong>Audit log monitoring</strong> — monitor these events in <strong>Organization Settings → Audit Log</strong> or via webhook:</p><table tabindex="0"><thead><tr><th>Event</th><th>Meaning</th></tr></thead><tbody><tr><td><code>sso.idp.login_success</code></td><td>User authenticated successfully via IdP</td></tr><tr><td><code>sso.idp.login_failure</code></td><td>IdP assertion rejected (see error code in payload)</td></tr><tr><td><code>sso.idp.jit_provision</code></td><td>New user created by JIT provisioning</td></tr><tr><td><code>sso.config.created</code></td><td>SSO configuration added</td></tr><tr><td><code>sso.config.updated</code></td><td>SSO configuration changed</td></tr><tr><td><code>sso.config.deleted</code></td><td>SSO configuration removed</td></tr><tr><td><code>sso.enforce.enabled</code></td><td>SSO enforcement turned on</td></tr><tr><td><code>sso.enforce.disabled</code></td><td>SSO enforcement turned off</td></tr></tbody></table><p><strong>Client secret handling (OIDC):</strong> Never share the OIDC client secret with end users or embed it in client-side code. LunaOS stores it encrypted at rest and never returns it in full via the API after initial save.</p><hr><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><p>If users cannot log in or you see SSO error codes, see the <a href="./troubleshooting.html">SSO Troubleshooting Guide</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/sso/admin-guide.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const adminGuide = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  adminGuide as default
};
