{
  "name": "lunaforge-timetravel",
  "version": "0.2.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p . --skipLibCheck",
    "clean": "rm -rf dist",
    "watch": "tsc -w -p . --skipLibCheck",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "lunaforge-core": "file:../lunaforge-core"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}