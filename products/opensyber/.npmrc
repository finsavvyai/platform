# ─── Supply chain hardening (UNC6426 + Trivy incident) ───────────────────────
# Block postinstall scripts by default. Packages that genuinely need them
# (e.g., esbuild, sharp) must be explicitly allowlisted below.
ignore-scripts=true

# Allowlist: packages that require install scripts to function
# Each entry has been manually verified as safe.
# Run `pnpm approve-builds` to manage this list interactively.
