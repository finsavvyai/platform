/**
 * Latest agent version — used by the API to detect outdated agents.
 * Bump this when a new agent release is published.
 */
export const LATEST_AGENT_VERSION = '0.2.0';

/** Default Docker image for the agent container. */
export const DEFAULT_AGENT_IMAGE = 'ghcr.io/opensyber/agent';
