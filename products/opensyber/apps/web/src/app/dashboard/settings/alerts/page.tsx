import { AlertPreferences } from '@/components/dashboard/alerts/AlertPreferences';

export const metadata = { title: 'Alert Preferences' };

export default function Page(): React.ReactElement {
  return <AlertPreferences />;
}
