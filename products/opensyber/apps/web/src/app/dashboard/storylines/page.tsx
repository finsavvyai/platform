import { StorylinesClient } from './StorylinesClient';

export const metadata = { title: 'Storyline Attack Reconstruction' };

export default function StorylinesPage(): React.ReactElement {
  return <StorylinesClient />;
}
