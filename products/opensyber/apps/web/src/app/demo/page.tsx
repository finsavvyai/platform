import DemoClient from './DemoClient';

export const metadata = {
  title: 'Live Demo — OpenSyber',
  description: 'Experience the OpenSyber security dashboard with simulated real-time data.',
};

export default function DemoPage() {
  return <DemoClient />;
}
