{
  "name": "@opensyber/claw-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "echo 'Playwright integration tests — run with: npx playwright test'",
    "test:integration": "npx playwright test",
    "clean": "rm -rf dist .turbo .wrangler"
  },
  "dependencies": {
    "hono": "^4.12.14",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.71.0"
  }
}
