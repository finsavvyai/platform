node_modules/
dist/
test-results/
playwright-report/
bin/pushci-*
!bin/pushci.js
/pushci
/pushci.exe
*.out
.DS_Store
.env
.dev.vars
api/.wrangler/
.playwright-mcp/
workers/releases/.wrangler/

# Claude Code local state
.claude/

# Luna session artifacts (dogfood reports, outreach drafts, promote packs)
.luna/

# PushCI per-run log dumps (created by pushci run on failures)
.pushci/logs/

# MCP registry credentials (per-machine, never commit)
.mcpregistry_github_token
.mcpregistry_registry_token

# Audit artifacts — generated by scripts/audit.sh with absolute paths
# embedded; re-generated on demand, not source of truth
audit/results/

# Clojure tooling caches (clojure-lsp / clj-kondo)
.lsp/
.clj-kondo/
