[allowlist]
description = "Ignore local secrets files and test fixtures"
paths = [
    '''.env''',
    '''api/\.dev\.vars''',
    '''\.mcpregistry_github_token''',
    '''\.mcpregistry_registry_token''',
    '''mobile/ios/Podfile\.lock''',
    '''mobile/ios/Pods/Manifest\.lock''',
]
regexes = [
    # Test fixture keys — deliberately fake or example values
    '''PUSHCI_TEST_''',
    '''example\.com''',
]
