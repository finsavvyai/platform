export const API_BASE_URL =
  import.meta.env.VITE_API_URL || 'https://api.pushci.dev';

export const WS_BASE_URL =
  import.meta.env.VITE_WS_URL || 'wss://api.pushci.dev';

export const APP_URL =
  import.meta.env.VITE_APP_URL || 'https://app.pushci.dev';

export const PADDLE_CLIENT_TOKEN =
  import.meta.env.VITE_PADDLE_CLIENT_TOKEN || '';
