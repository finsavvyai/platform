{
  "mcpServers": {
    "pushci": {
      "command": "pushci",
      "args": ["mcp"],
      "description": "AI-native zero-config CI/CD. Auto-detects your stack, runs CI locally for free. Replaces GitHub Actions with zero YAML."
    }
  }
}
