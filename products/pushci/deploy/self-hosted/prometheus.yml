global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: pushci-runner
    static_configs:
      - targets:
          - runner:9090
        labels:
          cluster: self-hosted
          component: runner
