name: reusable-nested-leaf
on:
  workflow_call:
    inputs:
      depth:
        required: true
        type: string
jobs:
  leaf:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: echo "Nested chain depth: ${{ inputs.depth }}>leaf"
