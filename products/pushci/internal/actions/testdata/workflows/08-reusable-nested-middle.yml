name: reusable-nested-middle
on:
  workflow_call:
    inputs:
      depth:
        required: true
        type: string
jobs:
  middle:
    uses: ./.github/workflows/08-reusable-nested-leaf.yml
    with:
      depth: "${{ inputs.depth }}>middle"
