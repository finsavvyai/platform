# PushCI - Product Hunt Launch Package

## Tagline (54 chars)

Stop paying for CI minutes. AI builds your pipeline.

## Description (260 chars)

Tired of YAML configs and surprise CI bills? PushCI auto-detects your stack,
generates pipelines with AI, and runs builds on YOUR machine. 19 languages,
40+ frameworks, zero config. One command: `npx pushci init`. Free tier included.

## Maker's Comment (~200 words)

Hey PH! I'm the maker of PushCI.

Last month I got a $340 GitHub Actions bill for a side project. A side project!
I spent more time debugging YAML workflows than writing actual code. That was
the breaking point.

I built PushCI over a weekend because I believe CI/CD should be invisible. You
shouldn't need a DevOps engineer to ship a Next.js app. You shouldn't pay
cloud prices to run tests on code that's already on your laptop.

PushCI scans your repo, detects your language/framework/deploy target, and
generates a complete pipeline -- no YAML, no config files, no vendor lock-in.

What makes it different: (1) AI self-healing -- when builds break, it diagnoses
and fixes the issue automatically. (2) Runs locally -- your machine, your
Docker, your cost. (3) One command setup -- `npx pushci init` and you're done.

We support 19 languages, 40+ frameworks, and 20 deploy targets today. The free
tier is generous and open source projects run free forever.

I'd love your feedback. What would make you switch from GitHub Actions?

## First Comment Strategy

Post within 5 minutes of launch. Share a real cost comparison screenshot:
"Here's my actual GitHub Actions bill vs PushCI running the same repo locally."
Follow up at +30min with a GIF of the `npx pushci init` flow.

## Key Screenshots to Prepare

1. Terminal: `npx pushci init` detecting stack and generating pipeline
2. Dashboard: build history with pass/fail timeline
3. AI diagnosis: self-healing fixing a broken build
4. Cost comparison: side-by-side monthly billing chart
5. Framework detection: grid showing supported stacks

## Launch Timing

- **Day**: Tuesday (highest engagement, least competition)
- **Time**: 12:01 AM PT (full 24-hour window)
- **Prep**: Schedule all social posts for 8 AM PT wave

## Hunter Outreach Template

Subject: Quick look at PushCI before Tuesday launch?

Hi [Name], I've been following your hunts -- you clearly get developer tools.

I built PushCI: AI-powered CI/CD that runs on your own machine instead of
the cloud. One command setup, zero YAML, 19 languages supported.

Would you be open to hunting it this Tuesday? Happy to give you early access
and a full walkthrough. Takes 2 minutes to try: `npx pushci init`

Thanks, [Your name]
