{
  "name": "require-prod-approval",
  "description": "Production deployments require at least two human approvals from separate users (separation of duties). Matches when environment is 'production' AND approvals count is below 2.",
  "effect": "deny",
  "combine": "and",
  "conditions": [
    { "path": "run.environment", "op": "equals", "value": "production" },
    { "path": "run.deploy.approvals", "op": "less_than", "value": 2 }
  ],
  "message": "Production deployments require two separate approvals. Request reviews in the PushCI dashboard before retrying."
}
