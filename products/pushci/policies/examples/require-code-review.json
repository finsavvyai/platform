{
  "name": "require-code-review",
  "description": "Block runs that have not received at least one approving code review. Reads run.review.approvals (integer, populated by the PushCI webhook worker from GitHub/GitLab/Gerrit).",
  "effect": "deny",
  "conditions": [
    { "path": "run.review.approvals", "op": "less_than", "value": 1 }
  ],
  "message": "At least one approving review is required before this run can merge."
}
