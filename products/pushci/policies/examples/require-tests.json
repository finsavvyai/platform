{
  "name": "require-tests",
  "description": "Block runs that did not execute tests. We look at run.tests.ran; the runner sets this to true after a test step finishes. If it's not true, this policy denies.",
  "effect": "deny",
  "combine": "or",
  "conditions": [
    { "path": "run.tests.ran", "op": "not_equals", "value": true }
  ],
  "message": "Tests are required. This run did not execute a test step — add 'test' to your .pushci.yml or run 'pushci test' locally."
}
