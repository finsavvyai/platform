{
  "name": "block-unapproved-base-image",
  "description": "Only allow container base images from an approved registry (ghcr.io/norlys/*). Uses a negative lookahead regex: anything NOT starting with ghcr.io/norlys/ is denied.",
  "effect": "deny",
  "conditions": [
    { "path": "run.image", "op": "regex", "value": "^(?!ghcr\\.io/norlys/).+" }
  ],
  "message": "Base image is not from the approved registry. Use ghcr.io/norlys/<image>:<tag>."
}
