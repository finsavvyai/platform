---
phase: 04-e2e-ci-hardening
plan: 04
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - apps/api/src/cron/compliance-scan.ts
  - apps/api/src/cron/group-cleanup.ts
  - apps/api/src/cron/guest-review.ts
  - apps/api/src/cron/scheduled-remediation.ts
  - apps/api/src/cron/scheduled-scans.ts
  - apps/api/src/cron/security-stack-scan.ts
  - apps/api/src/cron/tenant-health.ts
  - apps/api/src/cron/webhook-retry.ts
  - apps/api/src/cron/workflow-trigger.ts
  - apps/api/src/queues/alert-handler.ts
  - apps/api/src/queues/workflow-handler.ts
  - apps/api/src/queues/sync-handler.ts
autonomous: true
requirements: [HARD-05]

must_haves:
  truths:
    - "Every cron handler that iterates tenants calls assertOrgId(tenant.organizationId, '<HandlerName>') inside the for loop before any DB query"
    - "workflow-trigger.ts which uses raw D1 SQL has an assertOrgId call or an explicit documented exception comment"
    - "No cron handler silently continues when organizationId is null/missing"
    - "Queue processors alert-handler.ts, workflow-handler.ts, and sync-handler.ts call assertOrgId before any DB query"
  artifacts:
    - path: "apps/api/src/cron/compliance-scan.ts"
      provides: "assertOrgId guard per tenant iteration"
      contains: "assertOrgId"
    - path: "apps/api/src/cron/scheduled-remediation.ts"
      provides: "assertOrgId guard"
      contains: "assertOrgId"
    - path: "apps/api/src/cron/workflow-trigger.ts"
      provides: "assertOrgId guard or documented exception"
      contains: "assertOrgId"
    - path: "apps/api/src/queues/alert-handler.ts"
      provides: "assertOrgId guard in queue processor"
      contains: "assertOrgId"
    - path: "apps/api/src/queues/workflow-handler.ts"
      provides: "assertOrgId guard in queue processor"
      contains: "assertOrgId"
    - path: "apps/api/src/queues/sync-handler.ts"
      provides: "assertOrgId guard in queue processor"
      contains: "assertOrgId"
  key_links:
    - from: "apps/api/src/cron/compliance-scan.ts"
      to: "apps/api/src/lib/org-scope-assert.ts"
      via: "import { assertOrgId } from '../lib/org-scope-assert'"
      pattern: "assertOrgId"
    - from: "apps/api/src/queues/alert-handler.ts"
      to: "apps/api/src/lib/org-scope-assert.ts"
      via: "import { assertOrgId } from '../lib/org-scope-assert'"
      pattern: "assertOrgId"
---

<objective>
Add assertOrgId() guard calls to all 9 cron handlers and 3 queue processors that lacked org_id assertions. Each handler iterates tenants — the guard goes inside the for loop, after the tenant object is available but before any DB query or Graph API call.

Purpose: Close HARD-05 — prevent silent cross-org data operations in async workers and queue processors.
Output: 9 cron files + 3 queue processor files each containing assertOrgId import and guard call.
</objective>

<execution_context>
@/Users/shaharsolomon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shaharsolomon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-e2e-ci-hardening/04-RESEARCH.md
@.planning/phases/04-e2e-ci-hardening/04-03-SUMMARY.md

<interfaces>
<!-- assertOrgId — created in plan 04-03 -->
```typescript
// apps/api/src/lib/org-scope-assert.ts
export function assertOrgId(
  orgId: string | null | undefined,
  context: string
): asserts orgId is string {
  if (!orgId) {
    throw new Error(`[${context}] org_id scope required — no query may run without tenant context`);
  }
}
```

<!-- getAllActiveTenants return shape -->
```typescript
// packages/db/src/queries/tenants.ts
export async function getAllActiveTenants(db: DB) {
  return db.select().from(tenants).where(eq(tenants.status, 'active'));
}
// Returns: Array<{ id, organizationId, azureTenantId, displayName, ... }>
// organizationId is text('organization_id').notNull() — typed as string, not nullable
// But DB reality can return undefined for un-set rows — assertOrgId catches this
```

<!-- workflow-trigger.ts uses raw D1 SQL -->
<!-- Raw query: SELECT id, display_name FROM tenants WHERE status = 'active' -->
<!-- No organization_id in SELECT — must either add it to the query OR document exception -->
<!-- Recommendation: add organization_id to the raw SELECT, then assertOrgId -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assertOrgId to compliance-scan, group-cleanup, guest-review, scheduled-remediation, scheduled-scans (5 cron files)</name>
  <files>
    apps/api/src/cron/compliance-scan.ts,
    apps/api/src/cron/group-cleanup.ts,
    apps/api/src/cron/guest-review.ts,
    apps/api/src/cron/scheduled-remediation.ts,
    apps/api/src/cron/scheduled-scans.ts
  </files>
  <action>
For each of the 5 files:

1. Add import at top: `import { assertOrgId } from '../lib/org-scope-assert';`

2. Find the `for (const tenant of tenants)` loop (or equivalent forEach/map).

3. Add `assertOrgId(tenant.organizationId, '<HandlerName>');` as the FIRST line inside the loop body, before any DB query or API call. Use the handler's function name as the context string (e.g., 'ComplianceScan', 'GroupCleanup', 'GuestReview', 'ScheduledRemediation', 'ScheduledScans').

**Per-file notes:**
- `compliance-scan.ts`: `getAllActiveTenants` returns typed rows with `organizationId`. Add `assertOrgId(tenant.organizationId, 'ComplianceScan')` at top of the for loop.
- `group-cleanup.ts`: Check how tenants are fetched. If using `getAllActiveTenants`, same pattern. If using raw SQL, add `organization_id` to SELECT first.
- `guest-review.ts`: Same pattern as group-cleanup.
- `scheduled-remediation.ts`: Research note says this is a cross-org query by timestamp. Add `assertOrgId` after iterating individual tenant objects. If the query is global (not per-tenant loop), add a code comment: `// INFO: This is an intentional platform-wide query across all orgs — no per-tenant assertOrgId needed here.` and do NOT add assertOrgId (do not force a guard where there's no org context available).
- `scheduled-scans.ts`: Uses raw D1 SQL (`tenantsRes.results` with `{ id: string; azure_tenant_id: string }`). Must add `organization_id` to the SELECT columns and then call `assertOrgId(t.organization_id, 'ScheduledScans')`.

Do NOT change the logic of any handler — only add the import and the assertOrgId call. Each file must stay under 200 lines (all are currently < 60 lines).
  </action>
  <verify>
    <automated>cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && grep -l "assertOrgId" apps/api/src/cron/compliance-scan.ts apps/api/src/cron/group-cleanup.ts apps/api/src/cron/guest-review.ts apps/api/src/cron/scheduled-remediation.ts apps/api/src/cron/scheduled-scans.ts | wc -l</automated>
  </verify>
  <done>Output is "5" — all 5 files contain assertOrgId. TypeScript passes: npx tsc --noEmit -p apps/api/tsconfig.json shows no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add assertOrgId to security-stack-scan, tenant-health, webhook-retry, workflow-trigger (4 cron files)</name>
  <files>
    apps/api/src/cron/security-stack-scan.ts,
    apps/api/src/cron/tenant-health.ts,
    apps/api/src/cron/webhook-retry.ts,
    apps/api/src/cron/workflow-trigger.ts
  </files>
  <action>
Apply assertOrgId import + guard to the remaining 4 cron files:

- `security-stack-scan.ts`: Add import. Find tenant loop. Add `assertOrgId(tenant.organizationId, 'SecurityStackScan')` at top of loop.

- `tenant-health.ts`: Add import. Find tenant loop (iterates `tenants` array). Add `assertOrgId(tenant.organizationId, 'TenantHealth')` at top of loop.

- `webhook-retry.ts`: Webhook retry may not iterate tenants directly — it retries deliveries. If no tenant loop exists, add a comment: `// No per-org assertOrgId needed — webhook retry processes delivery records, not tenant queries.` and do NOT add assertOrgId (do not force a guard where there's no org context available).

- `workflow-trigger.ts`: Uses raw D1 SQL. Current SELECT: `SELECT id, display_name FROM tenants WHERE status = 'active'`. Change to: `SELECT id, display_name, organization_id FROM tenants WHERE status = 'active'`. Then add `assertOrgId(tenant.organization_id, 'WorkflowTrigger')` inside the for loop. This file has a documented comment about intentional raw D1 bypass — preserve that comment, just add the org_id field and assertion.

After all edits: run `npx tsc --noEmit -p apps/api/tsconfig.json` to confirm no TypeScript errors introduced. Pay attention to raw SQL return types — may need to update the inline type annotation from `{ id: string; display_name: string }` to `{ id: string; display_name: string; organization_id: string | null }`.
  </action>
  <verify>
    <automated>cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && grep -c "assertOrgId" apps/api/src/cron/security-stack-scan.ts apps/api/src/cron/tenant-health.ts apps/api/src/cron/workflow-trigger.ts && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>grep shows assertOrgId in all 3 handler files (webhook-retry may be 0 if no tenant loop — that is acceptable, document the exception). TypeScript check passes with no new errors. Total assertOrgId coverage: `grep -rl "assertOrgId" apps/api/src/cron/` returns 8-9 files.</done>
</task>

<task type="auto">
  <name>Task 3: Add assertOrgId to queue processors alert-handler, workflow-handler, sync-handler (HARD-05)</name>
  <files>
    apps/api/src/queues/alert-handler.ts,
    apps/api/src/queues/workflow-handler.ts,
    apps/api/src/queues/sync-handler.ts
  </files>
  <action>
For each of the 3 queue processor files:

1. Read the file to understand the message handling structure. Queue processors typically receive a `batch` of messages and iterate over them with `for (const message of batch.messages)`.

2. Add import at top: `import { assertOrgId } from '../lib/org-scope-assert';`

3. Locate where `organizationId` (or `orgId`) is extracted from the message payload. It may be on `message.body.organizationId`, `message.body.orgId`, or similar.

4. Add `assertOrgId(orgId, '<HandlerName>');` immediately after extracting the org ID, before any DB query or downstream processing. Use context strings: 'AlertHandler', 'WorkflowHandler', 'SyncHandler'.

**Per-file notes:**
- `alert-handler.ts`: Processes alert queue messages. Find where org/tenant context is available in the message body and assert before DB writes.
- `workflow-handler.ts`: Processes workflow execution messages. Same pattern — assert org before any workflow DB operations.
- `sync-handler.ts`: Processes tenant sync messages. Assert org before Graph API calls and DB cache writes.

If a queue processor receives messages that intentionally lack org context (e.g., platform-level system events), add a comment: `// INFO: Platform-level message — no per-org assertOrgId needed.` and skip the guard.

Do NOT change logic — only add import and assertOrgId call. Each file must stay under 200 lines.
  </action>
  <verify>
    <automated>cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && grep -l "assertOrgId" apps/api/src/queues/alert-handler.ts apps/api/src/queues/workflow-handler.ts apps/api/src/queues/sync-handler.ts | wc -l</automated>
  </verify>
  <done>Output is "2" or "3" — all queue processors with org context contain assertOrgId. TypeScript passes: npx tsc --noEmit -p apps/api/tsconfig.json. `grep -rl "assertOrgId" apps/api/src/queues/` returns 2-3 files.</done>
</task>

</tasks>

<verification>
Coverage check across all 9 cron files:

```bash
grep -l "assertOrgId" /Users/shaharsolomon/dev/projects/portfolio/tenantiq/apps/api/src/cron/*.ts
```

Expected: 8-9 files (webhook-retry may be excluded if no tenant iteration loop).

Coverage check across queue processors:
```bash
grep -l "assertOrgId" /Users/shaharsolomon/dev/projects/portfolio/tenantiq/apps/api/src/queues/*.ts
```

Expected: 2-3 files.

TypeScript gate:
```bash
cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | grep -c "error TS"
```
Expected: 0
</verification>

<success_criteria>
- 8-9 of 9 cron files contain `import { assertOrgId }` and a call to `assertOrgId(tenant.organizationId, '...')`
- webhook-retry.ts either contains assertOrgId OR a documented exception comment
- workflow-trigger.ts raw SQL updated to include organization_id in SELECT
- alert-handler.ts, workflow-handler.ts, sync-handler.ts each contain assertOrgId or documented exception
- TypeScript passes with no new errors
- All modified files remain under 200 lines
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-ci-hardening/04-04-SUMMARY.md` with:
- List of files modified and the exact assertOrgId call added to each
- Any files excluded (webhook-retry) and why
- Queue processor handling decisions
- TypeScript check result
</output>
