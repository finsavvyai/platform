---
phase: 04-e2e-ci-hardening
plan: 06
type: execute
wave: 2
depends_on: [04-02, 04-03, 04-04, 04-05]
files_modified:
  - tests/e2e/flows/msp-login.spec.ts
  - tests/e2e/flows/cis-scan.spec.ts
  - tests/e2e/flows/sso-provisioning.spec.ts
  - tests/e2e/flows/copilot-readiness.spec.ts
  - apps/web/src/routes/+page.svelte
  - apps/web/src/routes/security/cis/+page.svelte
  - apps/web/src/routes/security/copilot/+page.svelte
  - apps/web/src/routes/settings/+page.svelte
autonomous: false
requirements: [E2E-02, E2E-03, E2E-04, E2E-05]

must_haves:
  truths:
    - "All 12 E2E tests in tests/e2e/flows/ pass (GREEN) when run with Chromium"
    - "MSP login flow test confirms dashboard populates with mocked tenant data"
    - "CIS scan test confirms controls table renders and scan trigger is visible"
    - "Copilot Readiness test confirms readiness score is visible"
    - "SSO provisioning test confirms SSO connection list renders"
  artifacts:
    - path: "tests/e2e/flows/msp-login.spec.ts"
      provides: "GREEN E2E-02 tests"
    - path: "tests/e2e/flows/cis-scan.spec.ts"
      provides: "GREEN E2E-03 tests"
    - path: "tests/e2e/flows/sso-provisioning.spec.ts"
      provides: "GREEN E2E-04 tests"
    - path: "tests/e2e/flows/copilot-readiness.spec.ts"
      provides: "GREEN E2E-05 tests"
  key_links:
    - from: "tests/e2e/flows/copilot-readiness.spec.ts"
      to: "apps/web/src/routes/security/copilot/+page.svelte"
      via: "page.goto('/security/copilot') + locator('[data-testid=\"readiness-score\"]')"
      pattern: "data-testid"
    - from: "tests/e2e/flows/cis-scan.spec.ts"
      to: "apps/web/src/routes/security/cis/+page.svelte"
      via: "page.goto('/security/cis') + control list locator"
      pattern: "data-testid"
    - from: "tests/e2e/flows/msp-login.spec.ts"
      to: "apps/web/src/routes/+page.svelte"
      via: "page.goto('/') + locator('[data-testid=\"secure-score\"]')"
      pattern: "data-testid"
    - from: "tests/e2e/flows/sso-provisioning.spec.ts"
      to: "apps/web/src/routes/settings/+page.svelte"
      via: "page.goto('/settings') + locator('[data-testid=\"sso-connection-list\"]')"
      pattern: "data-testid"
---

<objective>
Turn all Wave 0 E2E stubs from RED to GREEN. Fix selectors to match actual component DOM structure, add missing data-testid attributes to web components where needed, and confirm all 12 E2E flow tests pass.

Purpose: Close E2E-02, E2E-03, E2E-04, E2E-05 — Playwright suite covers all new milestone features.
Output: 12 passing E2E tests. Human checkpoint confirms visual correctness.
</objective>

<execution_context>
@/Users/shaharsolomon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shaharsolomon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-e2e-ci-hardening/04-RESEARCH.md
@.planning/phases/04-e2e-ci-hardening/04-02-SUMMARY.md
@.planning/phases/04-e2e-ci-hardening/04-03-SUMMARY.md
@.planning/phases/04-e2e-ci-hardening/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix E2E selectors — discover actual DOM structure and update all 4 spec files</name>
  <files>
    tests/e2e/flows/msp-login.spec.ts,
    tests/e2e/flows/cis-scan.spec.ts,
    tests/e2e/flows/sso-provisioning.spec.ts,
    tests/e2e/flows/copilot-readiness.spec.ts,
    apps/web/src/routes/+page.svelte,
    apps/web/src/routes/security/cis/+page.svelte,
    apps/web/src/routes/security/copilot/+page.svelte,
    apps/web/src/routes/settings/+page.svelte
  </files>
  <action>
Run the failing E2E tests in headed mode (or inspect source) to discover the actual DOM selectors. Then update spec files and/or add data-testid attributes to web components.

**Step 1 — Discover actual selectors by reading the relevant Svelte component files:**
- `apps/web/src/routes/+page.svelte` — dashboard metrics
- `apps/web/src/routes/security/cis/+page.svelte` — CIS controls table
- `apps/web/src/routes/security/copilot/+page.svelte` — copilot readiness score
- `apps/web/src/routes/settings/+page.svelte` — SSO connections list

**Step 2 — For each failing selector, choose one of:**
a. Update the spec to use a more resilient selector (`text=`, `role=`, `aria-label=`) that already works
b. Add a `data-testid` attribute to the Svelte component if no stable selector exists

**Required data-testid attributes to add if missing:**
- `data-testid="readiness-score"` on the score display in `apps/web/src/routes/security/copilot/+page.svelte`
- `data-testid="cis-control-table"` on the controls table in `apps/web/src/routes/security/cis/+page.svelte`
- `data-testid="secure-score"` on the secure score metric in `apps/web/src/routes/+page.svelte`
- `data-testid="sso-connection-list"` on the SSO connections container in `apps/web/src/routes/settings/+page.svelte`

When adding data-testid to Svelte components: add attribute directly to the relevant element. These are test hooks — add them without changing visual behavior or styles. Each Svelte file must stay under 200 lines.

**Step 3 — Update spec files** to use the confirmed selectors (either text/role or newly added data-testid).

Run after each update: `npx playwright test tests/e2e/flows/ --project=chromium 2>&1 | tail -30`
  </action>
  <verify>
    <automated>cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npx playwright test tests/e2e/flows/ --project=chromium 2>&1 | tail -30</automated>
  </verify>
  <done>Playwright output shows "12 passed" (or the number of tests defined in Wave 0). No tests show "failed" or "timed out". All 4 spec files produce GREEN results.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 4 hardening is complete:
    - CSP + security headers wired in create-app.ts (HARD-01)
    - assertOrgId guard in 9 cron handlers + 3 queue processors (HARD-05)
    - 7 compound D1 indexes added + migration generated (HARD-06)
    - CI gate blocks on SAST + dep audit + Gitleaks (HARD-02, HARD-03, HARD-04)
    - playwright.config.ts uses wrangler pages dev (E2E-01)
    - 12 E2E tests GREEN across 4 flow specs (E2E-02–E2E-05)
  </what-built>
  <how-to-verify>
    1. Run full unit suite: `cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npm run test 2>&1 | tail -20`
       Expected: All tests pass, coverage thresholds met.

    2. Run E2E suite: `npx playwright test tests/e2e/flows/ --project=chromium 2>&1 | tail -20`
       Expected: 12 passed, 0 failed.

    3. Verify CI structure: `grep -A3 "status-check:" .github/workflows/ci.yml | head -10`
       Expected: needs array contains sast, dependency-audit, secret-scan.

    4. Verify security headers in running app (optional):
       Start API locally with `cd apps/api && npm run dev`, then:
       `curl -s -I http://localhost:8787/health | grep -i "content-security-policy\|x-frame"`
       Expected: CSP header present.

    5. Open browser at http://localhost:5173 (run `npm run dev` first):
       - Dashboard loads with tenant metrics
       - CIS Benchmark page at /security/cis shows controls
       - Copilot page at /security/copilot shows readiness score
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failures to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete gate:

```bash
# Unit tests
cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npm run test 2>&1 | tail -10

# E2E flows
cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npx playwright test tests/e2e/flows/ --project=chromium 2>&1 | grep -E "passed|failed"

# All E2E (regression)
cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npx playwright test --project=chromium 2>&1 | grep -E "passed|failed"

# Requirement coverage check
grep -l "assertOrgId" apps/api/src/cron/*.ts | wc -l  # expect 8-9
grep -l "assertOrgId" apps/api/src/queues/*.ts | wc -l  # expect 2-3
grep "wrangler pages dev" playwright.config.ts         # expect 1 line
grep "sast" .github/workflows/ci.yml                   # expect multiple lines
grep "dependency-audit" .github/workflows/ci.yml       # expect multiple lines
```
</verification>

<success_criteria>
- `npm run test` passes with coverage thresholds (90% lines, 85% branches)
- `npx playwright test tests/e2e/flows/ --project=chromium` shows 12 passed, 0 failed
- All existing Playwright tests from sso.spec.ts still pass (no regression)
- Human checkpoint approved
- Phase 4 complete: HARD-01 through HARD-06 + E2E-01 through E2E-05 all addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-ci-hardening/04-06-SUMMARY.md` with:
- Final test run results (unit + E2E)
- Selectors fixed per spec file
- data-testid attributes added to which components
- Any tests skipped and why
- Human checkpoint result
</output>
