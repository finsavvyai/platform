---
phase: 04-e2e-ci-hardening
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/security.yml
  - playwright.config.ts
autonomous: true
requirements: [HARD-02, HARD-03, HARD-04, E2E-01, E2E-05]

must_haves:
  truths:
    - "ci.yml status-check job includes sast, dependency-audit, and secret-scan in its needs array"
    - "security.yml includes a Gitleaks job alongside TruffleHog"
    - "playwright.config.ts webServer[0].command uses wrangler pages dev, not pnpm --filter web dev"
    - "ci.yml e2e job is already present and in status-check needs (E2E-05 confirmed)"
    - "ci.yml has a dependency-audit job that runs pnpm audit --audit-level=high (HARD-03)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Merge-blocking security gates in status-check"
      contains: "sast"
    - path: ".github/workflows/security.yml"
      provides: "Gitleaks secret scan job"
      contains: "gitleaks"
    - path: "playwright.config.ts"
      provides: "wrangler pages dev web server config"
      contains: "wrangler pages dev"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "sast/dependency-audit/secret-scan jobs"
      via: "status-check needs array"
      pattern: "needs:.*sast.*dependency-audit.*secret-scan"
---

<objective>
Wire security scan jobs into the CI merge gate and update playwright.config.ts to use wrangler pages dev. Three independent files — can be edited sequentially in one plan since they have no code dependencies.

Purpose: Close HARD-02 (CI SAST gate), HARD-03 (CI dependency vulnerability audit), HARD-04 (Gitleaks), E2E-01 (wrangler pages dev), E2E-05 (E2E in CI confirmed).
Output: ci.yml blocks merges on security failures; security.yml adds Gitleaks; playwright.config.ts uses correct web server.
</objective>

<execution_context>
@/Users/shaharsolomon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shaharsolomon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-e2e-ci-hardening/04-RESEARCH.md

<interfaces>
<!-- Current ci.yml status-check (lines 104-127) -->
```yaml
status-check:
  name: All Checks Passed
  runs-on: ubuntu-latest
  needs: [line-limit, lint, typecheck, test, e2e, build]
  # UPDATE needs to: [line-limit, lint, typecheck, test, e2e, build, sast, dependency-audit, secret-scan]
  if: always()
  steps:
    - name: Evaluate job results
      run: |
        results=(
          "${{ needs.line-limit.result }}"
          "${{ needs.lint.result }}"
          "${{ needs.typecheck.result }}"
          "${{ needs.test.result }}"
          "${{ needs.e2e.result }}"
          "${{ needs.build.result }}"
          # ADD: "${{ needs.sast.result }}"
          # ADD: "${{ needs.dependency-audit.result }}"
          # ADD: "${{ needs.secret-scan.result }}"
        )
```

<!-- Current playwright.config.ts webServer[0] -->
```typescript
webServer: [{
  command: 'pnpm --filter @tenantiq/web dev',  // CHANGE THIS
  url: 'http://localhost:5173',
  reuseExistingServer: !process.env.CI,
  timeout: 120_000,
  stdout: 'pipe',
  stderr: 'pipe',
}]
// Change command to:
// 'wrangler pages dev apps/web/.svelte-kit/cloudflare --port 5173 --compatibility-date 2024-09-23'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security jobs to ci.yml + add Gitleaks to security.yml</name>
  <files>.github/workflows/ci.yml, .github/workflows/security.yml</files>
  <action>
**ci.yml changes:**

1. Add 3 new job definitions BEFORE the `status-check` job (they must exist before being referenced in `needs`):

```yaml
sast:
  name: SAST (Semgrep)
  runs-on: ubuntu-latest
  container:
    image: semgrep/semgrep
  steps:
    - uses: actions/checkout@v4
    - name: Run Semgrep SAST
      run: semgrep scan --config=p/security-audit --error .

dependency-audit:
  name: Dependency Audit (pnpm audit)
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: ./.github/actions/setup
    - name: Audit dependencies
      run: pnpm audit --audit-level=high

secret-scan:
  name: Secret Scan (Gitleaks)
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Run Gitleaks
      uses: gitleaks/gitleaks-action@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

2. Update `status-check` job:
   - `needs`: add `sast`, `dependency-audit`, `secret-scan`
   - `results` array: add `"${{ needs.sast.result }}"`, `"${{ needs.dependency-audit.result }}"`, `"${{ needs.secret-scan.result }}"`

**security.yml changes** (the standalone security workflow — keep it, add Gitleaks for defense-in-depth):

Read the existing `security.yml` to find the `jobs:` section. Add a `gitleaks:` job to it as well (same Gitleaks action). This ensures Gitleaks runs in both the PR merge gate (ci.yml) AND the scheduled security workflow (security.yml).

ci.yml must stay under 200 lines. Currently it's ~127 lines; adding 3 jobs (~30 lines each = ~90 lines added) will bring it to ~217 lines. The 200-line cap applies to source files (`src/`, `app/`, `lib/`) per CLAUDE.md — GitHub Actions YAML files are infrastructure config, not source files. The 200-line cap does NOT apply to `.github/workflows/*.yml`. Document this exception if needed.
  </action>
  <verify>
    <automated>grep -E "sast|dependency-audit|secret-scan|gitleaks" /Users/shaharsolomon/dev/projects/portfolio/tenantiq/.github/workflows/ci.yml | wc -l</automated>
  </verify>
  <done>grep returns 12+ lines (job definitions + needs entries + results entries). ci.yml `status-check.needs` contains all 9 jobs. security.yml contains a gitleaks job. YAML is valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo OK`</done>
</task>

<task type="auto">
  <name>Task 2: Update playwright.config.ts to use wrangler pages dev (E2E-01)</name>
  <files>playwright.config.ts</files>
  <action>
Update `playwright.config.ts` line 56 `webServer[0].command`:

Change FROM:
```typescript
command: 'pnpm --filter @tenantiq/web dev',
```

Change TO:
```typescript
command: 'wrangler pages dev apps/web/.svelte-kit/cloudflare --port 5173 --compatibility-date 2024-09-23',
```

Leave all other fields unchanged (`url`, `reuseExistingServer`, `timeout`, `stdout`, `stderr`).

Leave `webServer[1]` (the API wrangler dev server) completely unchanged.

**Reasoning:** `wrangler pages dev` serves the Cloudflare Pages output and binds D1/KV/R2 — matches production behavior. The `pnpm build` step in the CI `e2e` job (already present: `- run: pnpm build`) generates `apps/web/.svelte-kit/cloudflare` before the web server starts. Locally, developers must run `pnpm build` before running E2E tests; `reuseExistingServer: !process.env.CI` allows them to reuse if already running.

Add a comment above the command:
```typescript
// wrangler pages dev: serves Cloudflare Pages output (production runtime).
// Run 'pnpm build' before E2E tests locally. CI e2e job already does this.
```
  </action>
  <verify>
    <automated>grep "wrangler pages dev" /Users/shaharsolomon/dev/projects/portfolio/tenantiq/playwright.config.ts</automated>
  </verify>
  <done>Output contains "wrangler pages dev apps/web/.svelte-kit/cloudflare --port 5173". TypeScript check passes: npx tsc --noEmit --project . (or equivalent) confirms playwright.config.ts has no type errors. File stays under 80 lines.</done>
</task>

</tasks>

<verification>
CI gate verification (structure check):
```bash
python3 -c "import yaml; d=yaml.safe_load(open('/Users/shaharsolomon/dev/projects/portfolio/tenantiq/.github/workflows/ci.yml')); n=d['jobs']['status-check']['needs']; print('needs:', n)"
```
Expected output: `needs: ['line-limit', 'lint', 'typecheck', 'test', 'e2e', 'build', 'sast', 'dependency-audit', 'secret-scan']`

Dependency audit verification (HARD-03):
```bash
grep -E "dependency-audit|audit-ci" /Users/shaharsolomon/dev/projects/portfolio/tenantiq/.github/workflows/ci.yml
```
Expected: lines showing the dependency-audit job definition and its inclusion in status-check needs.

Gitleaks verification:
```bash
grep "gitleaks" /Users/shaharsolomon/dev/projects/portfolio/tenantiq/.github/workflows/ci.yml /Users/shaharsolomon/dev/projects/portfolio/tenantiq/.github/workflows/security.yml
```
Expected: 2+ lines containing `gitleaks/gitleaks-action`

Playwright config verification:
```bash
grep "wrangler pages dev" /Users/shaharsolomon/dev/projects/portfolio/tenantiq/playwright.config.ts
```
Expected: 1 line matching.
</verification>

<success_criteria>
- ci.yml status-check.needs includes sast, dependency-audit, secret-scan
- ci.yml results array evaluates all 9 jobs
- ci.yml has sast, dependency-audit, secret-scan job definitions
- dependency-audit job runs `pnpm audit --audit-level=high` (HARD-03 coverage)
- security.yml has a gitleaks job
- playwright.config.ts webServer[0].command uses wrangler pages dev
- ci.yml YAML parses without error
- security.yml YAML parses without error
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-ci-hardening/04-05-SUMMARY.md` with:
- Changes made to each file (before/after for key lines)
- ci.yml final status-check needs array
- playwright.config.ts old command → new command
- YAML validity confirmed
</output>
