---
phase: 04-e2e-ci-hardening
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/e2e/flows/msp-login.spec.ts
  - tests/e2e/flows/cis-scan.spec.ts
  - tests/e2e/flows/sso-provisioning.spec.ts
  - tests/e2e/flows/copilot-readiness.spec.ts
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03, E2E-04, E2E-05]

must_haves:
  truths:
    - "Four E2E spec files exist in tests/e2e/flows/ directory"
    - "Each spec file contains failing tests (RED) — failing because data-testid selectors target non-existent or unverified elements"
    - "Existing sso.spec.ts pattern (page.route mocking) is followed in all new specs"
    - "setupAuthenticatedAdmin helper is reused (imported from shared fixture or inlined)"
  artifacts:
    - path: "tests/e2e/flows/msp-login.spec.ts"
      provides: "E2E-02 flow: MSP login → connect tenant → dashboard populated"
    - path: "tests/e2e/flows/cis-scan.spec.ts"
      provides: "E2E-03 flow: CIS scan trigger → results → auto-remediate"
    - path: "tests/e2e/flows/sso-provisioning.spec.ts"
      provides: "E2E-04 flow: SSO config → SSO login URL → provisioning signal"
    - path: "tests/e2e/flows/copilot-readiness.spec.ts"
      provides: "E2E-05 flow: Copilot Readiness trigger → score display"
  key_links:
    - from: "tests/e2e/flows/msp-login.spec.ts"
      to: "apps/web/src/routes/+page.svelte"
      via: "page.goto('/') and locator('[data-testid=\"dashboard-metric\"]')"
      pattern: "data-testid"
    - from: "tests/e2e/flows/copilot-readiness.spec.ts"
      to: "apps/web/src/routes/security/copilot/+page.svelte"
      via: "page.goto('/security/copilot') and readiness-score locator"
      pattern: "data-testid"
---

<objective>
Create Wave 0 E2E RED stubs for flows E2E-02 through E2E-05. Tests use page.route() mocking (no live IdP needed), follow the established sso.spec.ts pattern, and test selectors that may not yet have data-testid attributes — making them RED pending Wave 2 implementation.

Purpose: Define what "passing E2E" means before fixing selectors in Wave 2.
Output: Four spec files under tests/e2e/flows/ that run but fail on missing selectors.
</objective>

<execution_context>
@/Users/shaharsolomon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shaharsolomon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-e2e-ci-hardening/04-RESEARCH.md

<interfaces>
<!-- From tests/e2e/sso/sso.spec.ts — the established pattern -->
```typescript
// Auth helper pattern (reuse across all new specs)
async function setupAuthenticatedAdmin(page: Page, overrides = {}) {
  await page.route('**/api/auth/me', route => route.fulfill({
    status: 200, contentType: 'application/json',
    body: JSON.stringify({ user: { id: 'user-e2e', email: 'admin@e2e.test',
      organizationId: 'org-e2e', tenantIds: ['tenant-e2e'], role: 'admin',
      status: 'active', ...overrides } })
  }));
  await page.route('**/api/tenants', route => route.fulfill({
    status: 200, contentType: 'application/json',
    body: JSON.stringify({ tenants: [{ id: 'tenant-e2e', displayName: 'E2E Tenant',
      domain: 'e2e.onmicrosoft.com', status: 'active', lastSyncAt: null }] })
  }));
}

// Route mock pattern
await page.route('**/api/tenants/*/copilot/readiness', route =>
  route.fulfill({ status: 200, contentType: 'application/json',
    body: JSON.stringify({ score: 78, categories: { identity: 90, data: 65 } }) })
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: msp-login.spec.ts + cis-scan.spec.ts — E2E-02 and E2E-03 stubs</name>
  <files>tests/e2e/flows/msp-login.spec.ts, tests/e2e/flows/cis-scan.spec.ts</files>
  <action>
Create `tests/e2e/flows/` directory (if it does not exist) and write two spec files.

**msp-login.spec.ts** (covers E2E-02: MSP login → connect tenant → dashboard populated):
- `test.describe('MSP Login Flow')` with `setupAuthenticatedAdmin` before each
- Mock `**/api/tenants/*/dashboard` to return `{ secureScore: 72, totalUsers: 50, totalAlerts: 2, licenseCost: 5000, complianceScore: 88, mfaAdoption: 95, riskScore: 'low' }`
- Test 1: "dashboard loads with tenant data after MSP login" — goto('/'), expect `[data-testid="secure-score"]` toBeVisible (RED: testid may not exist yet)
- Test 2: "tenant display name is visible in sidebar or header" — goto('/'), expect `text=E2E Tenant` toBeVisible
- Test 3: "unauthenticated redirect to sign-in" — mock /api/auth/me → 401, goto('/'), expect sign-in text visible

**cis-scan.spec.ts** (covers E2E-03: CIS scan trigger → view results → auto-remediate):
- Mock `**/api/tenants/*/cis/scan` to return `{ jobId: 'job-1', status: 'started' }`
- Mock `**/api/tenants/*/cis/controls*` to return `{ controls: [{ id: 'cis-1', title: 'MFA Required', status: 'failing', severity: 'high', remediationSteps: ['Enable MFA'] }], total: 1, passing: 0, failing: 1 }`
- Mock `**/api/tenants/*/cis/remediate` to return `{ ok: true }`
- Test 1: "CIS benchmark page loads with control results" — goto('/security/cis'), expect `[data-testid="cis-control-table"]` or `text=MFA Required` visible
- Test 2: "failing controls are displayed with severity badge" — expect `text=high` or `[data-testid="severity-badge"]` visible
- Test 3: "scan trigger button is visible" — expect `button:has-text("Run Scan")` or `button:has-text("Trigger")` visible

Each file: under 60 lines. Follow sso.spec.ts import style exactly: `import { test, expect, type Page } from '@playwright/test'`.
  </action>
  <verify>
    <automated>cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npx playwright test tests/e2e/flows/msp-login.spec.ts tests/e2e/flows/cis-scan.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist. Playwright discovers all tests. Tests run and fail on missing selectors (not on syntax/import errors). Output shows test names visible with FAILED or timed-out status.</done>
</task>

<task type="auto">
  <name>Task 2: sso-provisioning.spec.ts + copilot-readiness.spec.ts — E2E-04 and E2E-05 stubs</name>
  <files>tests/e2e/flows/sso-provisioning.spec.ts, tests/e2e/flows/copilot-readiness.spec.ts</files>
  <action>
**sso-provisioning.spec.ts** (covers E2E-04: SSO config → login URL generated → provisioning signal):
Note: Cannot do real OAuth redirect in Playwright without live IdP. Tests verify: (a) SSO config visible in settings, (b) login initiation returns a redirect URL, (c) JIT provisioning API endpoint accepts a mock token.

- Mock `**/api/sso*` GET → `{ connections: [{ id: 'conn-1', provider: 'oidc', domain: 'corp.example.com', display_name: 'Corp OIDC', status: 'active' }] }`
- Mock `**/api/sso/login/*` → `{ redirectUrl: 'https://idp.example.com/authorize?state=abc' }` (200)
- Mock `**/api/auth/me` as admin
- Test 1: "SSO settings shows existing connection" — goto('/settings'), expect `text=corp.example.com` visible
- Test 2: "SSO login initiation returns redirect URL" — use `page.request.get('/api/sso/login/corp.example.com')` (hits API directly), BUT since Playwright API requests bypass page.route mocks, use `request` fixture and API_BASE. Expect non-500 response. Note: Real redirect URL only available with live IdP — test verifies endpoint is reachable.
- Test 3: "OIDC callback with missing state param returns error" — `request.get(API_BASE + '/api/sso/callback/oidc?code=test')` expect status !== 200 (reuse pattern from existing sso.spec.ts)

**copilot-readiness.spec.ts** (covers E2E-05: Copilot Readiness trigger → score display):
- Mock `**/api/tenants/*/copilot/readiness` → `{ score: 78, status: 'complete', categories: { identity: 90, dataProtection: 65, deviceHealth: 80 }, recommendations: ['Enable MFA for remaining users'] }`
- Mock `**/api/tenants/*/copilot/assess` → `{ jobId: 'assess-1', status: 'started' }`
- Mock `**/api/tenants/*/license-summary` → `{ copilotLicensed: 45, totalLicensed: 50 }`
- Test 1: "Copilot Readiness page loads with score" — goto('/security/copilot'), expect `[data-testid="readiness-score"]` or `text=78` visible
- Test 2: "category breakdown is visible" — expect `text=identity` or `text=Identity` visible
- Test 3: "Trigger assessment button is present" — expect `button:has-text("Assess")` or `button:has-text("Run")` visible

Each file: under 70 lines. Use `const API_BASE = process.env.API_BASE_URL ?? 'http://localhost:8787'` consistent with sso.spec.ts.
  </action>
  <verify>
    <automated>cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npx playwright test tests/e2e/flows/sso-provisioning.spec.ts tests/e2e/flows/copilot-readiness.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <done>Both files exist. Playwright discovers all tests. Tests run and produce failures on missing selectors (RED). No syntax or import errors.</done>
</task>

</tasks>

<verification>
All four E2E stub files exist under tests/e2e/flows/ and are discoverable by Playwright:

`cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npx playwright test tests/e2e/flows/ --list 2>&1 | head -30`

Expected: 10-12 test names listed across 4 files.
</verification>

<success_criteria>
- tests/e2e/flows/msp-login.spec.ts — 3 tests, covers E2E-02
- tests/e2e/flows/cis-scan.spec.ts — 3 tests, covers E2E-03
- tests/e2e/flows/sso-provisioning.spec.ts — 3 tests, covers E2E-04
- tests/e2e/flows/copilot-readiness.spec.ts — 3 tests, covers E2E-05
- All files follow sso.spec.ts import and mock patterns
- All files under 70 lines
- Playwright discovers all tests (`--list` shows 12 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/04-e2e-ci-hardening/04-02-SUMMARY.md` with:
- Files created and line counts
- Test names in each file
- RED confirmation (which selectors are expected to fail and why)
- Any deviations from the sso.spec.ts pattern
</output>
