---
phase: 02-frontend-completions
plan: "06"
type: execute
wave: 3
depends_on:
  - "02-03"
  - "02-04"
  - "02-05"
files_modified:
  - "# runner plan — no source files modified; fixes go back to earlier plans if needed"
autonomous: false
requirements:
  - COP-01
  - COP-02
  - COP-03
  - COP-04
  - COP-05
  - COP-06
  - SNAP-01
  - SNAP-02
  - SNAP-03
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
must_haves:
  truths:
    - "Full test suite is green before human review begins"
    - "All 13 Phase 2 requirements are visually verified by a human"
    - "No file in the phase exceeds 200 lines"
  artifacts:
    - path: ".planning/phases/02-frontend-completions/02-06-SUMMARY.md"
      provides: "Verification sign-off record"
  key_links: []
---

<objective>
Run the full automated test suite, verify all 13 requirements pass automated checks, then pause for human verification of the three visual behaviors that cannot be confirmed in jsdom.

Purpose: Gate Phase 2 completion on a passing test suite and human sign-off on visual correctness before /gsd:verify-work is called.
Output: Green test suite + human approval or issue list for rework.
</objective>

<execution_context>
@/Users/shaharsolomon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shaharsolomon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-completions/02-VALIDATION.md
@.planning/phases/02-frontend-completions/02-03-SUMMARY.md
@.planning/phases/02-frontend-completions/02-04-SUMMARY.md
@.planning/phases/02-frontend-completions/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and enforce coverage + line-count gates</name>
  <files><!-- runner task — reads outputs, does not write source files --></files>
  <action>
    Run the full test suite across both workspaces:
    ```bash
    cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npm run test
    ```

    If any test fails, identify the failure, fix the root cause in the relevant source file, and re-run. Do NOT proceed to the checkpoint until the full suite is green.

    Also run TypeScript compile checks:
    ```bash
    npx tsc --noEmit -p apps/api/tsconfig.json
    npx tsc --noEmit -p apps/web/tsconfig.json
    ```

    Run line-count check to confirm no file exceeded 200 lines:
    ```bash
    find /Users/shaharsolomon/dev/projects/portfolio/tenantiq/apps -name "*.ts" -o -name "*.svelte" | xargs wc -l 2>/dev/null | awk '$1 > 200 && $2 != "total" {print $0}' | grep -v node_modules | grep -v ".d.ts"
    ```
    If any file exceeds 200 lines, split it and update the test suite accordingly before proceeding.

    Confirm all required Phase 2 tests are green by running them individually and noting the pass result for each requirement:

    **SNAP-02 coverage evidence** (run explicitly to confirm SNAP-02):
    ```bash
    cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq/apps/api && npx vitest run src/lib/snapshots/diff.test.ts --reporter=verbose
    ```

    Full required test list:
    - `src/routes/copilot-readiness.test.ts` (COP-01 to COP-06 including /license-summary) — API workspace
    - `src/lib/snapshots/drift-detector.test.ts` (SNAP-03 snapshotId/baselineId) — API workspace
    - `src/lib/snapshots/diff.test.ts` (SNAP-02 diff algorithm correctness) — API workspace
    - `src/lib/components/dashboard/DriftSummaryWidget.test.ts` (SNAP-01) — web workspace
    - `src/lib/components/AlertCard.test.ts` (SNAP-03 frontend diff link) — web workspace
    - `src/lib/components/copilot/OversharingPanel.test.ts` (COP-03) — web workspace
    - `src/lib/components/copilot/LicenseSummaryPanel.test.ts` (COP-04/05) — web workspace
    - `src/lib/components/storage/ConsumersTable.test.ts` (STOR-02, STOR-04) — web workspace
  </action>
  <verify>
    <automated>cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npm run test 2>&1 | tail -30</automated>
  </verify>
  <done>Full test suite green. Both TypeScript projects compile clean. No file exceeds 200 lines. All 8 required test files listed above pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 2 frontend completions:

    COP-01/02: Copilot Readiness trigger and score display (pre-existing, confirmed working)
    COP-03: Oversharing risk signal panel on /security/copilot
    COP-04: Sensitivity label coverage panel on /security/copilot
    COP-05: Copilot-licensed vs total licensed panel on /security/copilot
    COP-06: PDF export from /security/copilot (pre-existing, confirmed working)
    SNAP-01: Drift summary widget on dashboard home (/)
    SNAP-02: Visual snapshot diff on /backups/config (pre-existing, confirmed working)
    SNAP-03: "View diff" link in Alerts feed for config_drift alerts
    STOR-01: Per-user OneDrive usage on /governance/storage (pre-existing, confirmed working)
    STOR-02: Top-20 consumers cap with "N of M users" header
    STOR-03: SharePoint site usage on /governance/storage (pre-existing, confirmed working)
    STOR-04: Per-row >=90% quota warning badge on ConsumersTable
  </what-built>
  <how-to-verify>
    Start the local dev server:
    ```bash
    cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npm run dev
    ```

    Then verify each of the three visual-only behaviors:

    **1. PDF export renders correctly (COP-06)**
    - Navigate to http://localhost:5173/security/copilot
    - If no assessment exists, click "Run Assessment" first
    - Click "Export PDF" (or the ExportMenu dropdown)
    - Verify: a new tab opens with a formatted HTML report and a browser print dialog (or the report HTML is styled correctly for printing)

    **2. Snapshot diff visual rendering (SNAP-02)**
    - Navigate to http://localhost:5173/backups/config/compare
    - Select two different snapshots from the dropdowns
    - Click "Compare"
    - Verify: diff rows appear with green background for added, red for removed, yellow/orange for changed items

    **3. Storage sort/filter interaction (STOR-02)**
    - Navigate to http://localhost:5173/governance/storage
    - Go to the OneDrive tab
    - Click each column header (User, Used, Usage%) and verify sort order changes and arrows appear
    - If more than 20 users exist, verify the header shows "20 of N users"
    - If any user has >=90% utilization, verify the orange/red "Over quota" badge appears in their row

    Type "approved" if all three pass, or describe which specific behavior is broken.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
Full suite:
```bash
cd /Users/shaharsolomon/dev/projects/portfolio/tenantiq && npm run test 2>&1 | tail -10
```
</verification>

<success_criteria>
- Full automated test suite green
- TypeScript compiles clean for both api and web
- No file exceeds 200 lines
- SNAP-02 diff.test.ts passes explicitly (requirement coverage confirmed)
- Human has approved all three visual behaviors or issues have been documented for follow-up
- Phase 2 can proceed to /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-completions/02-06-SUMMARY.md`
</output>
