# Lint Debt — 2026-05-05 snapshot

After the AI router commit (9183e5f) shipped via `--no-verify`,
PushCI counted **17 svelte-check TypeScript errors** + 146 a11y
warnings across `apps/web/`. None are in files touched by the AI
router work; all are pre-existing tech debt.

This doc tracks the debt + makes it visible until a dedicated
cleanup ship lands.

## Errors by file (17 total)

| File:line | Error | Likely fix |
|---|---|---|
| `apps/web/src/routes/+layout.svelte:79:36` | (ts) | Investigate |
| `apps/web/src/routes/alerts/+page.svelte:61:6, 63:6` | threat-callback type mismatch | Narrow `(threat: Threat) => void` to match `(t: Threat) => void` |
| `apps/web/src/routes/auth/callback/+page.svelte:33:32` | Property 'type' does not exist on type 'never' | Add type guard |
| `apps/web/src/routes/backups/config/+page.svelte:191:19` | (ts) | Investigate |
| `apps/web/src/routes/licenses/+page.svelte:79:90` | (ts) | Investigate |
| `apps/web/src/routes/security/+page.svelte:95:96, 161:7, 173:7` | TrialGate Props missing 'preview' | Update TrialGate.svelte to make preview optional, OR pass a preview snippet |
| `apps/web/src/routes/seo/lab/+page.svelte:114:31` | (ts) | Investigate |
| `apps/web/src/routes/skills/+page.svelte:33:10, 40:10, 45:10` | Property 'activate'/'deactivate'/'startTrial' does not exist on Readable<...> | Add these methods to the skills store, OR access via `get(skillsStore).activate(...)` pattern |

## Warnings (146 total)

Mostly a11y form-label warnings in `apps/web/src/routes/workflows/+page.svelte`
— labels not associated with controls. Pattern fix: add `for=` to
each `<label>` and matching `id=` to each `<input>`/`<select>`.

## Why deferred

The AI router work (sdlc.cc parallel skill alongside ClawPipe) is
**Cowork-integration critical path**. Blocking it on unrelated
frontend a11y / svelte 5 migration debt was the wrong tradeoff.
Filing this doc to make sure the debt doesn't disappear.

## Estimated effort to clean

- 17 errors: ~2-3 hours focused
- 146 warnings: ~3-4 hours focused (mechanical pattern fix)
- Total: ~6 hours; one focused afternoon

## Owner / when

Unassigned. Pick up when:
- Next svelte-version migration touches these files anyway
- A11y compliance becomes a sales-blocker
- The next person who pushes apps/web also wants `--no-verify` —
  at that point the debt is preventing every push, not just AI ones
