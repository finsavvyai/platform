import {
	LayoutDashboard, HeartPulse, Bell, CreditCard, ScrollText, Workflow,
	AlertTriangle, Activity, Mail, ShieldCheck, KeyRound,
	Bot, HardDrive, History,
	Building2, Shield, BarChart3, Lock, Fingerprint
} from 'lucide-svelte';
import type { ComponentType } from 'svelte';

export const iconMap: Record<string, ComponentType> = {
	LayoutDashboard,
	HeartPulse,
	Bell,
	CreditCard,
	ScrollText,
	Workflow,
	AlertTriangle,
	Activity,
	Mail,
	ShieldCheck,
	KeyRound,
	Bot,
	HardDrive,
	History,
	Building2,
	Shield,
	BarChart3,
	Lock,
	Fingerprint,
};
