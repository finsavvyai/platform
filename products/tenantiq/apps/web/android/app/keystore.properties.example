# Android release signing config.
# Copy to keystore.properties (gitignored) and fill in real values.
# Generate the .keystore via: scripts/android-setup-keystore.sh

storeFile=tenantiq-release.keystore
storePassword=CHANGE_ME
keyAlias=tenantiq
keyPassword=CHANGE_ME
