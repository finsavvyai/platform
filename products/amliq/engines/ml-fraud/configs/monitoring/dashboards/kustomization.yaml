apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: quantumbeam-monitoring-dashboards
  namespace: monitoring

resources:
  - grafana/

configurations:
  - grafana/kustomizeconfig.yaml

commonLabels:
  app.kubernetes.io/name: quantumbeam-dashboards
  app.kubernetes.io/component: monitoring
  app.kubernetes.io/part-of: quantumbeam

namespace: monitoring