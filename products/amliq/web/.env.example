VITE_API_URL=http://localhost:8080
VITE_APP_NAME=AMLIQ Dashboard
VITE_ENVIRONMENT=development

# Deploy-time only — set these in CI secrets, never in a local .env file.
# Used by `wrangler pages deploy` in the CI pipeline.
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
