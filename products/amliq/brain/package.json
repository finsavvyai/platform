{
  "name": "@finsavvyai/amliq-brain",
  "version": "0.1.0",
  "private": true,
  "description": "AMLIQ Brain — agent-driven AML/CFT intelligence layer (Brain SKU). Sibling to AMLIQ Investigate (api/, web/, engines/).",
  "type": "module",
  "main": "./services/api/dist/index.js",
  "types": "./services/api/dist/index.d.ts",
  "exports": {
    "./api": {
      "types": "./services/api/dist/index.d.ts",
      "import": "./services/api/dist/index.js"
    },
    "./retrieval": {
      "types": "./services/retrieval/dist/index.d.ts",
      "import": "./services/retrieval/dist/index.js"
    },
    "./sanctions": {
      "types": "./services/sanctions/dist/index.d.ts",
      "import": "./services/sanctions/dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf services/*/dist *.tsbuildinfo"
  },
  "dependencies": {
    "hono": "4.12.23",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vitest/coverage-v8": "1.6.1",
    "typescript": "^5.5.0",
    "vitest": "1.6.1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
