{
  "mcpServers": {
    "amliq": {
      "command": "./bin/aegis-mcp",
      "args": [],
      "env": {
        "DATABASE_URL": "postgres://postgres:postgres@localhost:5432/aegis?sslmode=disable"
      },
      "description": "AMLIQ sanctions screening — screen entities, check PEPs, analyze transactions"
    }
  }
}
