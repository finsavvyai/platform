{
  "name": "@amliq/investigate-decision",
  "version": "0.1.0",
  "private": true,
  "description": "AMLIQ unified Investigate Decision API: orchestrates QuantumBeam + ml-fraud engines, emits tamper-evident audit. Standalone TS package (not in pnpm workspace; install with --ignore-workspace).",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "4.6.14"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "@vitest/coverage-v8": "1.6.1",
    "typescript": "5.5.4",
    "vitest": "1.6.1"
  },
  "engines": {
    "node": ">=20"
  }
}
