# Directories excluded from CI checks (line limit, lint)
# One path per line, relative to repo root

# Legacy Java scheduler (v1, not subject to 100-line rule)
cmd/schedulers-stage

# Claude Code skills (external data files)
.claude/skills
