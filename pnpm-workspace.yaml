packages:
  - "packages/*"
  - "infrastructure/observability"
  - "websites/*"
  # products/* and oss/* deliberately excluded — migrated trees carry
  # their own dep graphs (vitest 4.x, etc.) and would conflict with the
  # canonical workspace. Per round-4 rules, only add specific packages
  # back here once they import @finsavvyai/* directly.
