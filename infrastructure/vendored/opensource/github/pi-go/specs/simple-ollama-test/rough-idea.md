# Rough Idea

Simple test for pi with actual ollama using `pi --model minimax-m2.5:cloud`.

Create a practical, end-to-end test that exercises the pi CLI tool against a real Ollama backend with the minimax-m2.5:cloud model. This validates that the pi agent can connect to Ollama, send prompts, and receive responses correctly.
