{
  "name": "@finsavvyai/shared-types",
  "version": "0.1.0",
  "description": "Cross-product type contracts: AML decisions, fraud scores, audit events, common identifiers.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "1.6.1"
  }
}
