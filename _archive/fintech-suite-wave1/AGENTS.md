## Learned User Preferences
- Prefer project-wide execution for Luna flows when prompted for scope (e.g., `all`).
- Use the Luna requirements -> design -> planning workflow for this workspace.

## Learned Workspace Facts
- The workspace maintains Luna planning artifacts in `.luna/fintech-suite/`.
- Core Luna docs currently present are `requirements.md`, `design.md`, and `implementation-plan.md`.
- The workspace includes a custom heal command at `.cursor/commands/ll-heal.md`.
- The heal workflow includes bootstrap/report assets at `.cursor/scripts/ll-heal-init.sh` and `.luna/fintech-suite/heal-report.template.md`.
