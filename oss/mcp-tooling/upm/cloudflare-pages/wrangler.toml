# Cloudflare Pages Configuration for UPM Website
name = "upm-website"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"

# Environment variables
[vars]
BACKEND_URL = "http://34.29.39.106:8040"
ENVIRONMENT = "production"

# Custom domain configuration
# Note: Domains are managed via Cloudflare Dashboard or wrangler pages domain commands