{
  "name": "upm-website",
  "version": "1.0.0",
  "description": "UPM Website - Cloudflare Pages",
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name=upm-website",
    "deploy:production": "wrangler pages deploy public --project-name=upm-website --env production",
    "dev": "wrangler pages dev public",
    "prepare": "../scripts/prepare-cloudflare-pages.sh"
  },
  "devDependencies": {
    "wrangler": "^3.22.1"
  }
}
