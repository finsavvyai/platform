{
  "name": "clawpipe-booster-benchmark",
  "version": "0.1.0",
  "description": "Public benchmark of ClawPipe Booster — deterministic-skip rate, latency, quality regression across agent / chat-RAG / structured-extraction workloads.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "pull:corpora": "tsx scripts/pull-all.ts",
    "smoke": "tsx bench/run.ts --smoke",
    "bench": "tsx bench/run.ts",
    "summarize": "tsx bench/summarize.ts",
    "test": "tsx --test bench/quality.test.ts",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4",
    "typescript": "^5"
  }
}
