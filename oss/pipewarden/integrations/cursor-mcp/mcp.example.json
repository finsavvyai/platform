{
  "mcpServers": {
    "pipewarden": {
      "command": "pipewarden",
      "args": ["mcp"],
      "env": {
        "PIPEWARDEN_DATABASE_PATH": "${HOME}/.pipewarden/db.sqlite",
        "PIPEWARDEN_LOG_LEVEL": "warn"
      }
    }
  }
}
