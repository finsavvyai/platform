// Package middleware provides HTTP middleware for the PipeWarden server.
// Production middleware (Chain, CORS, SecurityHeaders, RequestLogger, RecoverPanic,
// RequestID, ContentType) is in production.go.
// Auth middleware is in auth.go.
package middleware
