apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipewarden.fullname" . }}
  labels: {{- include "pipewarden.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "pipewarden.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
