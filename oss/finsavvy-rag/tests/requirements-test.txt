# Test-only dependencies for the finsavvy-rag suite.
#
# The suite is hermetic: it stubs `sentence_transformers` and patches
# `sqlalchemy.create_engine`, so the heavy/slow runtime deps (torch,
# transformers, sentence-transformers) and the Postgres driver
# (psycopg2-binary) are deliberately NOT installed here. This keeps CI fast
# and removes the need for a live Postgres or a model download.
#
# These mirror the *light* subset of services/rag + services/gateway
# requirements that the app imports at module load / request time.
fastapi>=0.111.0
httpx>=0.27.0
pydantic>=2.8.0
SQLAlchemy>=2.0.30
numpy>=1.26.0
pytest>=8.0.0
